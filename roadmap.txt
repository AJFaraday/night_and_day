bugs:
- jumping while going through doors
- stuck on bottom of screen (no death event)

features:
- Simple deploy script
- death event
- Level select (accept level param, present list of levels)
- Dynamic background
- hint messages on screen
- default map elements (box, door, player etc) - expanded with level specific
- ledge hanging
- "butt slam" - R. Burgess


notes:

// do something after n milliseconds
player.active = false;
this.time.delayedCall(500, function () {
  player.active = true;
});